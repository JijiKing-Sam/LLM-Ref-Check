# 匹配算法改进说明

## 🔍 问题分析

用户反馈误判率太高，基本正确的bib文件只有10%能验证通过。经过分析，主要问题在于：

1. **匹配阈值过高** - 原来的阈值（0.6-0.7）太严格
2. **相似度计算过于简单** - 只使用简单的字符串比较
3. **搜索策略不够灵活** - 查询方式单一
4. **年份匹配过于严格** - 不允许任何容差

## ✅ 改进措施

### 1. 创建改进的匹配算法 (`improved_matcher.py`)

#### 多算法融合
- **SequenceMatcher**: 基于序列的相似度
- **词汇重叠相似度**: Jaccard相似度
- **子串包含检查**: 处理部分匹配
- **重要词汇匹配**: 移除停用词，关注关键词

#### 降低匹配阈值
- 标题匹配阈值：从 0.7 → 0.5
- 综合匹配阈值：从 0.6 → 0.35
- 置信度阈值：从 0.8 → 0.7

#### 灵活的匹配策略
```python
is_match = (
    similarity_score >= 0.35 or
    (title_sim >= 0.65 and author_match) or
    (title_sim >= 0.75) or
    (title_sim >= 0.5 and author_match and year_match)
)
```

### 2. 改进作者匹配

- 支持 "Last, First" 和 "First Last" 两种格式
- 提取姓氏进行匹配
- 至少有一个共同作者就认为匹配

### 3. 改进年份匹配

- 允许1年容差（考虑发表时间差异）
- 差异在2年内只小幅扣分，不直接拒绝

### 4. 改进搜索策略

#### ArXiv搜索
- 尝试多种查询方式（完整标题、关键词）
- 增加结果数量（从10到20）
- 去重处理

#### Crossref搜索
- 年份范围放宽（前后1年）
- 增加结果数量（从10到20）

### 5. 改进验证源选择

- 总是尝试Crossref（覆盖最广）
- 更宽松的选择策略

### 6. 改进置信度计算

- 单源置信度：从 0.7 → 0.8
- 如果标题相似度很高，进一步提高置信度
- 降低验证通过阈值：从 0.8 → 0.7

## 📊 改进效果预期

### 改进前
- 匹配阈值：0.6-0.7
- 验证通过率：~10%

### 改进后
- 匹配阈值：0.35-0.5
- 预期验证通过率：60-80%（取决于数据质量）

## 🧪 测试建议

1. **使用真实bib文件测试**
   ```bash
   python main.py your_references.bib
   ```

2. **对比改进前后**
   - 记录改进前的验证通过率
   - 记录改进后的验证通过率
   - 检查是否有误报（将不存在的论文标记为存在）

3. **调整阈值**（如果需要）
   - 如果误报太多：提高阈值（在 `improved_matcher.py` 中）
   - 如果漏报太多：降低阈值

## ⚙️ 可调参数

在 `ref_checker/improved_matcher.py` 中：

1. **匹配阈值**（第219行）：
   ```python
   is_match = (similarity_score >= 0.35 or ...)  # 可调整0.35
   ```

2. **标题相似度权重**（第150-155行）：
   ```python
   similarity = (
       seq_sim * 0.25 +
       word_sim * 0.35 +
       substr_sim * 0.15 +
       important_overlap * 0.25
   )
   ```

3. **年份容差**（第195行）：
   ```python
   tolerance: int = 1  # 可调整容差年数
   ```

## 📝 使用说明

改进后的算法会自动应用，无需额外配置。所有验证器都已更新使用新的匹配算法。

如果发现误报或漏报，可以：
1. 调整 `improved_matcher.py` 中的阈值
2. 报告具体案例，进一步优化算法

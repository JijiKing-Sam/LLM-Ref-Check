# 🚀 推送代码到 GitHub - 最终步骤

## 当前状态

✅ 仓库已创建：https://github.com/JijiKing-Sam/LLM-Ref-Check  
✅ 所有代码已准备好（23个文件）  
⏳ 需要 GitHub 认证才能推送

## 方法 1：使用 Personal Access Token（推荐）

### 步骤 1：生成 Token

1. 访问：**https://github.com/settings/tokens**
2. 点击 **"Generate new token"** -> **"Generate new token (classic)"**
3. 填写信息：
   - **Note**: `LLM-Ref-Check Push`
   - **Expiration**: 选择 90 days 或更长
   - **Scopes**: ✅ 勾选 **`repo`**（完整仓库权限）
4. 点击 **"Generate token"**
5. **复制生成的 token**（⚠️ 只显示一次，请保存好）

### 步骤 2：推送代码

运行脚本：
```bash
cd /jet/home/xma8/LLM-Ref-Check
bash push_with_token.sh
```

然后输入刚才复制的 token。

---

## 方法 2：直接在命令中使用 Token

```bash
cd /jet/home/xma8/LLM-Ref-Check

# 替换 YOUR_TOKEN 为你的实际 token
git remote set-url origin https://YOUR_TOKEN@github.com/JijiKing-Sam/LLM-Ref-Check.git
git push -u origin main

# 推送完成后，移除token（安全考虑）
git remote set-url origin https://github.com/JijiKing-Sam/LLM-Ref-Check.git
```

---

## 方法 3：使用 GitHub CLI（如果已安装）

```bash
gh auth login
cd /jet/home/xma8/LLM-Ref-Check
git push -u origin main
```

---

## 方法 4：配置 SSH 密钥（长期方案）

如果你想使用 SSH 方式（更安全，不需要每次输入token）：

1. 生成 SSH 密钥（如果还没有）：
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. 复制公钥：
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. 添加到 GitHub：
   - 访问：https://github.com/settings/keys
   - 点击 "New SSH key"
   - 粘贴公钥内容
   - 保存

4. 切换远程仓库为 SSH：
   ```bash
   cd /jet/home/xma8/LLM-Ref-Check
   git remote set-url origin git@github.com:JijiKing-Sam/LLM-Ref-Check.git
   git push -u origin main
   ```

---

## ✅ 推送成功后

访问：**https://github.com/JijiKing-Sam/LLM-Ref-Check**

你应该能看到：
- ✅ README.md
- ✅ 所有源代码文件
- ✅ 文档文件
- ✅ 配置文件

---

## ❓ 遇到问题？

**问题：Permission denied**
- 检查 token 是否正确
- 确认 token 有 `repo` 权限

**问题：Repository not found**
- 确认仓库已创建
- 确认仓库名正确：`LLM-Ref-Check`

**问题：Authentication failed**
- 重新生成 token
- 检查 token 是否过期

需要帮助？告诉我具体的错误信息！

# 多源验证功能说明

## 🎯 新增功能

项目已扩展支持多个可靠的学术数据源，实现多源交叉验证，大幅提高验证准确性和覆盖率。

## 📊 支持的数据源

### 1. **ArXiv** ✅
- **用途**: CS领域预印本论文
- **优势**: 官方API，稳定可靠
- **覆盖**: 计算机科学、数学、物理等预印本

### 2. **Crossref** ✅ (新增)
- **用途**: 期刊和会议论文（通过DOI）
- **优势**: 官方API，覆盖1.5亿+文献
- **覆盖**: 几乎所有期刊和会议论文
- **API**: 免费，无需密钥

### 3. **OpenReview** ✅ (新增)
- **用途**: 机器学习会议论文
- **优势**: 官方API，专门针对ML会议
- **覆盖**: NeurIPS, ICML, ICLR, AAAI, ACL等

### 4. **Google Scholar** ⚠️ (新增，可选)
- **用途**: 通用学术搜索
- **优势**: 覆盖面广
- **注意**: 使用第三方库，需要遵守使用条款
- **依赖**: `scholarly` 库

## 🔄 多源交叉验证逻辑

### 验证策略

1. **智能选择验证源**
   - 有ArXiv ID → 优先使用ArXiv
   - 有DOI → 优先使用Crossref
   - 会议论文 → 使用OpenReview
   - 其他 → 使用所有可用源

2. **置信度计算**
   - **单源验证**: 置信度 = 相似度 × 0.7（降低）
   - **双源验证**: 置信度 = 平均相似度 × 1.2（提高）
   - **多源验证**: 置信度 = 平均相似度 × (1.0 + 0.2 × (源数-1))（进一步提高）

3. **验证结果判定**
   - **≥2个源确认 + 置信度≥80%**: ✅ 验证通过（多源确认）
   - **≥1个源确认 + 置信度≥80%**: ✅ 验证通过
   - **≥2个源确认 + 置信度≥60%**: 基本可信（多源确认）
   - **≥1个源确认 + 置信度≥50%**: 基本可信，建议复查
   - **所有源都未找到**: ⚠️ 存在疑问，需要人工检查

## 🚀 使用方法

### 基本使用（自动选择所有源）

```python
from ref_checker.multi_validator import MultiSourceValidator
from ref_checker.bibtex_parser import BibTeXParser

# 初始化多源验证器
validator = MultiSourceValidator()

# 解析和验证
parser = BibTeXParser()
entries = parser.parse_file("references.bib")
results = validator.validate_batch(entries)
```

### 指定验证源

```python
# 只使用ArXiv和Crossref
validator = MultiSourceValidator(enable_sources=['arxiv', 'crossref'])

# 只使用Crossref（适合期刊论文）
validator = MultiSourceValidator(enable_sources=['crossref'])

# 只使用OpenReview（适合会议论文）
validator = MultiSourceValidator(enable_sources=['openreview'])
```

## 📦 安装依赖

### 必需依赖
```bash
pip install requests  # 已包含在requirements.txt中
```

### 可选依赖（Google Scholar）
```bash
pip install scholarly
```

**注意**: `scholarly` 库是可选的，如果不安装，Google Scholar验证功能将不可用，但不影响其他验证源。

## ⚙️ 配置

### API限制和延迟

各数据源的默认延迟设置：
- **ArXiv**: 0.5秒
- **Crossref**: 0.5秒（免费API，建议遵守礼貌使用原则）
- **OpenReview**: 0.5秒
- **Google Scholar**: 2.0秒（避免被限制）

### 自定义延迟

```python
from ref_checker.crossref_validator import CrossrefValidator

# 自定义Crossref延迟
crossref = CrossrefValidator(delay=1.0)  # 1秒延迟
```

## 📊 验证结果格式

每个验证源返回的结果包含：

```python
{
    'found': bool,              # 是否找到匹配
    'matched_result': dict,     # 匹配的论文信息
    'match_details': {         # 匹配详情
        'title_match': bool,
        'author_match': bool,
        'year_match': bool,
        'similarity_score': float
    },
    'source_id': str,          # 数据源ID（DOI、ArXiv ID等）
    'source_url': str,         # 数据源URL
    'verification_method': str, # 验证方法
    'error': str              # 错误信息（如果有）
}
```

## 🎯 使用场景

### 场景1: CS预印本论文
- **推荐源**: ArXiv
- **配置**: `enable_sources=['arxiv']`

### 场景2: 期刊论文
- **推荐源**: Crossref
- **配置**: `enable_sources=['crossref']`

### 场景3: 会议论文（ML领域）
- **推荐源**: OpenReview + Crossref
- **配置**: `enable_sources=['openreview', 'crossref']`

### 场景4: 全面验证（推荐）
- **推荐源**: 所有可用源
- **配置**: `enable_sources=None` (默认)

## ⚠️ 注意事项

1. **API限制**
   - Crossref: 免费但建议遵守礼貌使用原则
   - ArXiv: 有请求频率限制
   - Google Scholar: 可能被限制，建议谨慎使用

2. **网络连接**
   - 所有验证都需要网络连接
   - 建议在稳定的网络环境下使用

3. **验证时间**
   - 多源验证会增加验证时间
   - 每个源都有延迟设置，批量验证时请耐心等待

4. **准确性**
   - 多源交叉验证大幅提高准确性
   - 但仍有误报可能，重要论文建议人工复查

## 🔮 未来扩展

计划添加的数据源：
- [ ] DBLP（计算机科学书目数据库）
- [ ] PubMed（生物医学）
- [ ] IEEE Xplore API
- [ ] ACM Digital Library API

## 📝 示例

查看 `test_example.py` 了解基本用法，或直接运行：

```bash
python main.py your_references.bib
```

系统会自动使用多源验证（如果可用）。

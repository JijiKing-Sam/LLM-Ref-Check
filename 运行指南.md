# 🚀 运行指南

## 快速开始

### 1. 安装依赖

```bash
# 进入项目目录
cd /jet/home/xma8/LLM-Ref-Check

# 激活 conda 环境（如果使用）
conda activate /jet/home/xma8/conda_envs/cirag0

# 安装依赖（使用 python -m pip 确保安装到正确环境）
python -m pip install -r requirements.txt
```

### 2. 运行界面

#### 方式 A：运行增强版界面（推荐）⭐

```bash
streamlit run app_enhanced.py
```

#### 方式 B：运行原版界面

```bash
streamlit run app.py
```

#### 方式 C：使用启动脚本

```bash
# 编辑 run_web.sh，将 app.py 改为 app_enhanced.py（可选）
bash run_web.sh
```

### 3. 访问界面

运行后，终端会显示类似信息：
```
You can now view your Streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.x.x:8501
```

在浏览器中打开显示的 URL（通常是 `http://localhost:8501`）

## 📋 详细步骤

### 步骤 1：环境准备

```bash
# 确保在正确的 conda 环境中
conda activate /jet/home/xma8/conda_envs/cirag0

# 检查 Python 版本（需要 3.7+）
python --version

# 检查依赖是否已安装
python -c "import streamlit; import plotly; import pandas; print('✅ 所有依赖已安装')"
```

### 步骤 2：安装依赖（如果还没安装）

```bash
# 安装所有依赖
python -m pip install -r requirements.txt

# 或者单独安装
python -m pip install streamlit plotly pandas bibtexparser arxiv requests tqdm
```

### 步骤 3：运行应用

```bash
# 运行增强版（推荐）
streamlit run app_enhanced.py

# 或者运行原版
streamlit run app.py
```

### 步骤 4：使用界面

1. **上传文件**：在"文件上传"标签页上传 `.bib` 文件
2. **或粘贴文本**：在"文本输入"标签页直接粘贴 BibTeX 内容
3. **点击验证**：点击"🚀 开始验证"按钮
4. **查看结果**：查看验证结果、图表和详细信息

## 🎯 功能对比

| 功能 | 原版 (app.py) | 增强版 (app_enhanced.py) |
|------|--------------|------------------------|
| 文件上传 | ✅ | ✅ |
| 文本输入 | ✅ | ✅ |
| 验证功能 | ✅ | ✅ |
| 统计展示 | 基础 | 卡片式 + 可靠性分数 |
| 数据可视化 | ❌ | ✅ 饼图 + 直方图 |
| 交互式选择 | ❌ | ✅ |
| UI 设计 | 基础 | 现代化 |

## ⚙️ 配置选项

### 指定端口

```bash
streamlit run app_enhanced.py --server.port 8502
```

### 指定地址（允许外部访问）

```bash
streamlit run app_enhanced.py --server.address 0.0.0.0
```

### 同时指定端口和地址

```bash
streamlit run app_enhanced.py --server.port 8501 --server.address 0.0.0.0
```

## 🐛 常见问题

### Q: ModuleNotFoundError: No module named 'streamlit'

**解决方法**：
```bash
# 确保在正确的环境中
conda activate /jet/home/xma8/conda_envs/cirag0

# 使用 python -m pip 安装
python -m pip install streamlit
```

### Q: ModuleNotFoundError: No module named 'plotly'

**解决方法**：
```bash
python -m pip install plotly pandas
```

### Q: 端口被占用

**解决方法**：
```bash
# 使用其他端口
streamlit run app_enhanced.py --server.port 8502
```

### Q: 无法访问界面

**解决方法**：
```bash
# 使用 0.0.0.0 地址
streamlit run app_enhanced.py --server.address 0.0.0.0
```

### Q: 验证速度慢

**原因**：ArXiv API 有请求频率限制，工具已内置延迟机制

**解决方法**：这是正常现象，请耐心等待

## 📝 测试运行

### 使用示例文件测试

```bash
# 运行测试脚本
python test_example.py

# 或使用命令行工具测试
python main.py example.bib
```

## 🔧 高级用法

### 后台运行

```bash
# 使用 nohup
nohup streamlit run app_enhanced.py --server.port 8501 > streamlit.log 2>&1 &

# 查看日志
tail -f streamlit.log
```

### 使用 systemd 服务

参考 `DEPLOY.md` 文件中的 systemd 配置

## 📊 性能优化

1. **启用缓存**：Streamlit 会自动缓存函数结果
2. **限制并发**：在 `.streamlit/config.toml` 中配置
3. **使用生产模式**：设置环境变量 `STREAMLIT_SERVER_HEADLESS=true`

## 🎉 快速命令总结

```bash
# 一键运行（增强版）
cd /jet/home/xma8/LLM-Ref-Check && \
conda activate /jet/home/xma8/conda_envs/cirag0 && \
python -m pip install -r requirements.txt && \
streamlit run app_enhanced.py
```

## 📚 更多信息

- 详细安装说明：查看 `INSTALL.md`
- 部署指南：查看 `DEPLOY.md`
- 快速开始：查看 `QUICKSTART.md`

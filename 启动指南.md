# 🚀 启动指南

## 方式一：Web可视化界面（推荐）

### 快速启动

```bash
cd /jet/home/xma8/LLM-Ref-Check
conda activate /jet/home/xma8/conda_envs/cirag0
streamlit run app_enhanced.py
```

### 或者使用启动脚本

```bash
cd /jet/home/xma8/LLM-Ref-Check
conda activate /jet/home/xma8/conda_envs/cirag0
bash run_web.sh
```

### 访问界面

启动后，浏览器会自动打开，或手动访问：
- 本地访问：`http://localhost:8501`
- 远程访问：`http://your-server-ip:8501`

### 功能特点

- ✅ 可视化界面，操作简单
- ✅ 实时显示验证进度
- ✅ 交互式图表和统计
- ✅ 详细验证结果展示
- ✅ 支持文件上传和文本输入

---

## 方式二：命令行界面

### 基本用法

```bash
cd /jet/home/xma8/LLM-Ref-Check
conda activate /jet/home/xma8/conda_envs/cirag0
python main.py your_references.bib
```

### 指定输出文件

```bash
python main.py your_references.bib -o report.md
```

### 显示详细日志

```bash
python main.py your_references.bib -v
```

### 输出示例

验证完成后会生成Markdown格式的报告，包含：
- 📊 验证摘要统计
- 📋 每条参考文献的详细验证结果
- ⚠️ 问题和警告
- 🔍 字段级验证详情（新功能）
- ⚠️ 幻觉检测结果（新功能）

---

## 新版本功能

### ✨ 字段级验证

- 逐个验证标题、作者、年份、DOI
- 区分完全匹配和部分匹配
- 显示作者匹配详情

### 🎯 幻觉检测

- 检测标题匹配但作者不匹配的幻觉
- 检测标题和第一作者匹配但其他作者不匹配的幻觉
- 标记疑似幻觉引用

### 📊 增强的报告

- 显示作者匹配统计（完全匹配数、部分匹配数）
- 列出缺失作者
- 标记疑似幻觉引用
- 详细问题和警告

---

## 环境要求

- Python 3.8+
- 已安装所有依赖（见 `requirements.txt`）
- 激活正确的conda环境

## 故障排除

### 如果遇到模块导入错误

```bash
# 确保在正确的conda环境中
conda activate /jet/home/xma8/conda_envs/cirag0

# 重新安装依赖
python -m pip install -r requirements.txt
```

### 如果Web界面无法访问

- 检查防火墙设置
- 确认端口8501未被占用
- 尝试使用 `--server.port 8502` 指定其他端口

---

## 更多信息

- 详细文档：`字段级验证说明.md`
- 改进说明：`改进说明.md`
- GitHub仓库：https://github.com/JijiKing-Sam/LLM-Ref-Check
